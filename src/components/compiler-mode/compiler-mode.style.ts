import { makeStyles } from '@material-ui/core/styles';

export const useOptionWrapperStyles = makeStyles({
  root: {
    display: 'flex',
  },
});

export const useLabelInputStyles = makeStyles({
  root: {
    marginRight: '8px',
  },
});

export const useOptionFieldsStyles = makeStyles({
  root: {
    marginRight: '16px',
  },
});
